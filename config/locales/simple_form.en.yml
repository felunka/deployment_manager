en:
  simple_form:
    "yes": 'Yes'
    "no": 'No'
    required:
      text: 'required'
      mark: '*'
      # You can uncomment the line below if you need to overwrite the whole required html.
      # When using html, text and mark won't be used.
      # html: '<abbr title="required">*</abbr>'
    error_notification:
      default_message: "Please review the problems below:"
    options:
      defaults:
        node_status:
          pending_init: Pending initialization
          init_failed: Initialization failed!
          healthy: Healthy
          connection_lost: Connection to node lost!
          decommissioned: Decommissioned
        deployment_type:
          simple_docker_run: Simple docker run
          simple_docker_compose: Simple docker compose (paste compose below)
          github_action_runner: GitHub actions runner
        deployment_status:
          pending_init: Pending initialization
          init_failed: Initialization failed!
          healthy: Healthy
          connection_lost: Connection to deployment/node lost!
          decommissioned: Decommissioned
          
    # Examples
    # labels:
    #   defaults:
    #     password: 'Password'
    #   user:
    #     new:
    #       email: 'E-mail to sign in.'
    #     edit:
    #       email: 'E-mail.'
    # hints:
    #   defaults:
    #     username: 'User name to sign in.'
    #     password: 'No special characters, please.'
    # include_blanks:
    #   defaults:
    #     age: 'Rather not say'
    # prompts:
    #   defaults:
    #     age: 'Select your age'
